type IVideo = {
    title: string,
    thumbnail: string,
    channel: string,
    views: number,
    timestamp: string,
    duration: string,
    avatar: string,
    videoURL: string
    description?: string
}

export default IVideo;